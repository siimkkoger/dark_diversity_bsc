The code used is here. However as I was working with those files alone, then I didn't really think about structure (so they are very messy).

You can however find all NMF and autoencoder code in those files.